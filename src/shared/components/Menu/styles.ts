import styled from "styled-components";

export const Container = styled.div`
  ol {
    display: flex;
    align-items: baseline;
  }

  .menu {
    margin: 15px 10px;
    font-size: 12px;
    border: none;
  }

  .item1-button {
    margin: 0 20px 0 0;
  }

  .menu .btn:hover {
    font-size: 14px;
    cursor: pointer;
  }
`;
